# Forest Night — generated by ~/.rice/bin/apply-theme from ~/.rice/themes/forest-night/theme.toml.
# Included from ~/.config/sway/config via `include ~/.rice/sway/theme.conf`.

# Gaps: small breathing room between windows and screen edges
gaps inner 6
gaps outer 4
# `gaps inner/outer` above only sets the default for workspaces created
# AFTER a reload — already-open workspaces keep their old gap value
# otherwise. Force it onto everything currently open too.
exec_always swaymsg 'gaps inner all set 6' && swaymsg 'gaps outer all set 4'

# class                     border      bg          text        indicator   child_border
client.focused              #2f6b44   #060b07  #2f6b44   #2f6b44   #2f6b44
client.focused_inactive     #1f3725    #060b07  #6a7c6e  #1f3725    #1f3725
client.unfocused            #18291c   #060b07  #4b584e   #18291c   #18291c
client.urgent                #e0605a    #060b07  #e0605a    #e0605a    #e0605a
client.placeholder          #060b07  #060b07  #2f6b44   #060b07  #060b07
client.background           #060b07
