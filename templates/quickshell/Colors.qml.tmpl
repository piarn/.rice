// {{name}} — generated by ~/.rice/bin/apply-theme from ~/.rice/themes/{{theme}}.toml.
// Imported as the "Colors" singleton from ~/.config/quickshell/*.qml via
// `import quickshell` — resolved through QML2_IMPORT_PATH=$HOME/.rice, set
// in ~/.config/environment.d/10-quickshell.conf (quoted-string relative
// imports like `import "../../.rice/quickshell"` don't work here: quickshell
// loads configs into a virtual qs:/ resource tree, so `..` never escapes
// ~/.config/quickshell).
pragma Singleton
import QtQuick

QtObject {
    readonly property color black: "{{black}}"
    readonly property color surface: "{{surface}}"
    readonly property color fg: "{{fg}}"
    readonly property color white: "{{white}}"

    readonly property color neon: "{{neon}}"
    readonly property color acid: "{{acid}}"
    readonly property color dim: "{{dim}}"
    readonly property color deep: "{{deep}}"
    readonly property color gray: "{{gray}}"
    readonly property color gray2: "{{gray2}}"

    readonly property color red: "{{red}}"
    readonly property color amber: "{{amber}}"
    readonly property color blue: "{{blue}}"
    readonly property color magenta: "{{magenta}}"
    readonly property color cyan: "{{cyan}}"
}
