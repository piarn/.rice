#:schema https://yazi-rs.github.io/schemas/theme.json
# Crimson — generated by ~/.rice/bin/apply-theme from ~/.rice/themes/crimson/theme.toml.
#
# Source of truth for ~/.config/yazi/theme.toml, which is a symlink to this
# file via ~/.dots/yazi (TOML has no include directive, so unlike the other
# ~/.rice apps this file *is* the config, not something sourced from it).
#
# Only touches UI chrome (borders, status bar, tabs, modes, markers, popups)
# and the broad filetype.rules mime groups — never the per-extension
# icon/brand-color tables ([icon].{globs,dirs,files,exts,conds}), same call
# as leaving code syntax highlighting alone in the nvim theme.

[flavor]
dark  = ""
light = ""

[app]
overall = {}

# : Manager

[mgr]
cwd = { fg = "#a8313f" }

find_keyword  = { fg = "#d9954f", bold = true, italic = true, underline = true }
find_position = { fg = "#c14fa8", bg = "reset", bold = true, italic = true }

symlink_target = { italic = true }

marker_copied   = { fg = "#d94f5c", bg = "#d94f5c" }
marker_cut      = { fg = "#ff4d4d", bg = "#ff4d4d" }
marker_marked   = { fg = "#4fada0", bg = "#4fada0" }
marker_selected = { fg = "#d9954f", bg = "#d9954f" }
marker_symbol   = "│"

count_copied   = { fg = "#140808", bg = "#a8313f" }
count_cut      = { fg = "#140808", bg = "#ff4d4d" }
count_selected = { fg = "#140808", bg = "#d9954f" }

border_symbol = "│"
border_style  = { fg = "#4a1f22" }

syntect_theme = ""

# : Tabs

[tabs]
active   = { fg = "#140808", bg = "#a8313f", bold = true }
inactive = { fg = "#8a706d", bg = "#140808" }

sep_inner = { open = "", close = "" }
sep_outer = { open = "", close = "" }

# : Mode

[mode]
normal_main = { fg = "#140808", bg = "#a8313f", bold = true }
normal_alt  = { fg = "#a8313f", bg = "#140808" }

select_main = { fg = "#140808", bg = "#ff4d4d", bold = true }
select_alt  = { fg = "#ff4d4d", bg = "#140808" }

unset_main = { fg = "#140808", bg = "#d9954f", bold = true }
unset_alt  = { fg = "#d9954f", bg = "#140808" }

# : Indicator of hovered file

[indicator]
parent  = { reversed = true }
current = { reversed = true }
preview = { underline = true }
padding = { open = "", close = "" }

# : Status bar

[status]
overall   = {}
sep_left  = { open = "", close = "" }
sep_right = { open = "", close = "" }

perm_sep   = { fg = "#4a1f22" }
perm_type  = { fg = "#a8313f" }
perm_read  = { fg = "#d94f5c" }
perm_write = { fg = "#ff4d4d" }
perm_exec  = { fg = "#4fada0" }

progress_label  = { bold = true }
progress_normal = { fg = "#a8313f", bg = "#140808" }
progress_error  = { fg = "#d9954f", bg = "#ff4d4d" }

# : Which

[which]
cols            = 3
mask            = { bg = "#140808" }
cand            = { fg = "#a8313f" }
rest            = { fg = "#4a1f22" }
desc            = { fg = "#c14fa8" }
separator       = "  "
separator_style = { fg = "#4a1f22" }

# : Confirmation

[confirm]
border     = { fg = "#a8313f" }
title      = { fg = "#a8313f" }
body       = {}
list       = {}
btn_yes    = { reversed = true }
btn_no     = {}
btn_labels = [ "  [Y]es  ", "  (N)o  " ]

# : Spotter

[spot]
border = { fg = "#a8313f" }
title  = { fg = "#a8313f" }

tbl_col  = { fg = "#d94f5c" }
tbl_cell = { fg = "#d9954f", reversed = true }

# : Notification

[notify]
title_info  = { fg = "#a8313f" }
title_warn  = { fg = "#d9954f" }
title_error = { fg = "#ff4d4d" }

icon_info  = ""
icon_warn  = ""
icon_error = ""

# : Picker

[pick]
border   = { fg = "#a8313f" }
active   = { fg = "#d94f5c", bold = true }
inactive = {}

# : Input

[input]
border   = { fg = "#a8313f" }
title    = {}
value    = {}
selected = { reversed = true }

# : Completion

[cmp]
border   = { fg = "#a8313f" }
active   = { reversed = true }
inactive = {}

icon_file    = ""
icon_folder  = ""
icon_command = ""

# : Task manager

[tasks]
border  = { fg = "#a8313f" }
title   = {}
hovered = { fg = "#d94f5c", bold = true }

# : Help menu

[help]
on      = { fg = "#4fada0" }
run     = { fg = "#c14fa8" }
desc    = {}
hovered = { reversed = true, bold = true }
footer  = { fg = "#140808", bg = "#a8313f" }

# : File-specific styles (broad mime groups only — see note at top)

[filetype]
rules = [
	{ mime = "image/*", fg = "#c14fa8" },
	{ mime = "{audio,video}/*", fg = "#c14fa8" },
	{ mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", fg = "#ff4d4d" },
	{ mime = "application/{pdf,doc,rtf}", fg = "#4fada0" },
	{ mime = "vfs/{absent,stale}", fg = "#6b5452" },
	{ url = "*", is = "orphan", bg = "#ff4d4d" },
	{ url = "*", is = "exec"  , fg = "#a8313f" },
	{ url = "*", is = "dummy", bg = "#ff4d4d" },
	{ url = "*/", is = "dummy", bg = "#ff4d4d" },
	{ url = "*/", fg = "#a8313f" },
]
